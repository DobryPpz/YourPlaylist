# Lookup table
{
	"userRoleId":	int,	@id	@db.ObjectId
	"user":			User,	@relation(fields: [user_id], references: [email])	# One-to-Many
	"user_id":		String,	@db.ObjectId
	"role":			Role,	@relation(fields: [role_name], references: [name])	# One-to-Many
	"role_name":	String,	@db.ObjectId
	"room":			Room,	@relation(fields: [room_id], references: [roomId])	# One-to-Many
	"room_id"		int,	@db.ObjectId
}